<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>

<body class="bg-dark">
    <div class="container  mx-auto">
        <h1 class="text-info text-center">Best Dashboard Action</h1>

        <table class="boarder table table-striped ">
            <thead>
                <tr>
                    <th>S/N</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Password</th>
                    <th>Del/Edit</th>
                </tr>
            </thead>
            <% if (userDetails.length> 0) { %>
                <% for( let index=0; index < userDetails.length; index++ ) { %>
                    <tbody>
                        <tr>
                            <td>
                                <%= index+1 %>
                            </td>
                            <td>
                                <%= userDetails[index].firstname %>
                            </td>
                            <td>
                                <%= userDetails[index].lastname %>
                            </td>
                            <td>
                                <%= userDetails[index].email %>
                            </td>
                            <td>
                                <%= userDetails[index].password %>
                            </td>

                            <td>
                                <div class="d-flex">
                                    <form action="/delete" method="post">
                                        <input type="hidden" value=<%=userDetails[index].email %> name="newEmail">
                                        <button class="bg-danger">Delete</button>
                                    </form>


                                    <form action="/edit" method="post">
                                        <input type="hidden" value=<%=userDetails[index].email %> name="newEmail">
                                        <button class="bg-warning">Edit</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    </tbody>


                    <% } %>
                        <% } else { %>
                            <div class="alert text-center alert-danger">No user found</div>
                            <% } %>
        </table>
    </div>

    <a href="signup">Go to sign-up</a>
</body>

</html>